// import {ReturnModelType} from '@typegoose/typegoose';
// import Subscription from '../subscription.schema';
// import {GraphQLError} from 'graphql';

export default async function updateSubscription() {
// subscriptionModel: ReturnModelType<typeof Subscription>,
// input: {}
  // try {
  //   const subscription = await subscriptionModel.findById(input.id);
  //   if (!subscription) {
  //     throw new Error('Subscription not found');
  //   } else {
  //     return await subscriptionModel
  //       .findByIdAndUpdate(input.id, {...input}, {new: true})
  //       .populate(['associatedAgent']);
  //   }
  // } catch (error) {
  //   console.log(error);
  //   throw new GraphQLError(error);
  // }
}
